'use client';
import {useEffect,useState} from 'react';
import {api,setSession} from '../lib/session';
export default function LoginGate(){const [open,setOpen]=useState(false),[mode,setMode]=useState<'login'|'register'>('login'),[name,setName]=useState(''),[mobile,setMobile]=useState(''),[password,setPassword]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{const show=(event:Event)=>{const target=event.target as Element|null;if(target?.closest('[data-login]')){event.preventDefault();event.stopPropagation();setOpen(true)}};const openGate=()=>setOpen(true);document.addEventListener('click',show,true);window.addEventListener('godavari-login',openGate);return()=>{document.removeEventListener('click',show,true);window.removeEventListener('godavari-login',openGate);}},[]);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setMessage('');try{const response=await fetch(`${api()}/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(mode==='register'?{fullName:name,mobile,password}:{mobile,password})}),data=await response.json();if(!response.ok)throw Error(data.error);setSession(data);setOpen(false);location.reload();}catch(error:any){setMessage(error.message||'Could not sign in.');}finally{setBusy(false)}};if(!open)return null;return <div style={{position:'fixed',inset:0,zIndex:1000,background:'#0009',display:'grid',placeItems:'center',padding:20}}><form onSubmit={submit} style={{position:'relative',width:'min(420px,100%)',background:'#fffdfa',padding:28,borderRadius:14,boxShadow:'0 20px 50px #0006'}}><button type="button" onClick={()=>setOpen(false)} style={{position:'absolute',right:14,top:8,border:0,background:'transparent',fontSize:25}}>×</button><h2>{mode==='login'?'Welcome back':'Create your account'}</h2><p>{mode==='login'?'Use your mobile number and password.':'Your details are saved for future orders.'}</p>{mode==='register'&&<label>Full name<input required value={name} onChange={e=>setName(e.target.value)} /></label>}<label>Mobile number<input required inputMode="numeric" value={mobile} onChange={e=>setMobile(e.target.value)} placeholder="10-digit mobile number" /></label><label>Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters" /></label><button className="primary" style={{width:'100%'}} disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button><p>{mode==='login'?'New here? ':'Already have an account? '}<button type="button" onClick={()=>{setMode(mode==='login'?'register':'login');setMessage('')}}>{mode==='login'?'Create account':'Sign in'}</button></p>{message&&<p className="checkout-message">{message}</p>}</form></div>}
